<div class="container-fluid related-artists-container">
    <h5>
        <strong>Related Artists</strong>
    </h5>
    <div>
        {{#each relatedArtists.artists}}
        <div class="margin-top-1">
            <img src="{{this.images.2.url}}" class="album-image image-thumbnail"/>
            <a href="/artist/{{this.id}}" class="link padding-left-10">{{this.name}}</a>
        </div>
        {{/each}}
    </div>
</div>